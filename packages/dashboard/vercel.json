{
  "buildCommand": "cd ../.. && npm install && cd packages/dashboard && npx next build",
  "outputDirectory": ".next",
  "installCommand": "echo 'Skipping — handled in buildCommand'",
  "framework": "nextjs",
  "devCommand": "npx next dev",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- . ../../packages/scanner/ ../../package.json"
}
